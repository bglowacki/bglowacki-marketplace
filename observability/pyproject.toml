[project]
name = "observability"
version = "2.4.6"
description = "Claude Code session analysis plugin"
requires-python = ">=3.10"
dependencies = ["pyyaml"]

[project.optional-dependencies]
dev = ["pytest"]

[tool.uv]
dev-dependencies = [
    "pytest",
    "pytest-cov>=7.0.0",
]
